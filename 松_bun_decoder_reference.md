# 松ワープロ .bun ファイル デコーダ技術リファレンス（改訂版）

> 作成: 2026-06-28 (Claude Opus 4.6)  
> 改訂: 2026-06-28 — BUN2TX.C（村井安雄氏, 1986年）のソース照合により修正  
> 目的: 今後同形式のファイルを復号する際のClaude向け技術メモ

---

## 0. 一次資料：BUN2TX.C

`BUN2TX.C`（村井安雄氏, 1986年, ver 0.80286）は、showさんの`MTOT.C`を改変した新松→テキスト変換ツールのソースである。これが現存する唯一の信頼できる一次資料。ただしコメントに明記されているとおり**「新松」専用**であり、「松86はファイル形式が根本的に異なるらしいので従来通りMTOTを使ってください」とある。

→ **バージョンによってフォーマットが異なる**。以下では両形式を対比する。

---

## 1. バージョン別フォーマット比較

| 項目 | 新松（BUN2TX.C対象） | 松86/松5/松6（実測） |
|------|---------------------|---------------------|
| 本文開始オフセット | `0x10A`（= 266 dec） | `0x019E`（= 414 dec） |
| 本文レコードサイズ | **2バイト固定** | **8バイト**（文字2B + 属性6B） |
| 属性情報 | 別領域に格納 | 各文字レコードに埋め込み |
| 文書終了マーカ | `high == 0x84` | 未確認（区切り後EOF？） |
| BUN2TX対応 | ○ | ×（動作しない） |

---

## 2. ヘッダ構造（共通）

| オフセット | 内容 |
|-----------|------|
| 0x0000 | `01 4D` マジックバイト |
| 0x0002 | `01 06` バージョン情報？ |
| 0x0007〜0x002D | **タイトル文字列 (Shift-JIS / CP932)** ← BUN2TXのコメントより「7バイト目から40バイトが標題」 |

```python
title = data[0x07:0x2F].decode('cp932', errors='replace').rstrip('\x00')
```

---

## 3. 松86/松5/松6 の本文構造（実測）

### 3.1 本文開始の手がかり

```
0x0196〜0x019C: 00 84 が4個連続（区切りマーカ）
0x019E:         00 80（改行）← 本文はここから
```

### 3.2 ★核心：8バイトレコード + バイトスワップJIS★

```
[ char_lo ] [ char_hi ] [ 04 ] [ attr1 ] [ 00 ] [ attr2 ] [ 00 ] [ attr3 ]
   ↑JIS第2B   ↑JIS第1B    ←──────── 属性 6バイト ────────→
```

典型例: `5b 31 04 95 00 98 00 a1` → `越`（JIS `31 5b`、バイト逆順格納）

**JISバイト変換:**
```python
jis_hi = data[offset + 1]   # ファイルの2バイト目 → JIS第1バイト
jis_lo = data[offset + 0]   # ファイルの1バイト目 → JIS第2バイト
esc = b'\x1b$B' + bytes([jis_hi, jis_lo]) + b'\x1b(B'
char = esc.decode('iso-2022-jp')
```

### 3.3 制御コード（highバイトで判定）

BUN2TX.Cのコードから抜粋し、松86でも同様と推定される:

| highの値 | 意味 | 対処 |
|---------|------|------|
| `0x80` | 改行 | `\n` 出力 |
| `0x81` | 改ページ | `\f` 出力 |
| `0x82` | タブ | `\t` 出力 |
| `0x83` | デシマルタブ | `\t` 出力 |
| `0x84` | 文書終了（新松）／区切りマーカ（松86） | break または スキップ |
| `0x85` | 改段 | `\f` 出力 |
| `0x86` | 条件付改段 | `\f` 出力 |
| `0x87` | 奇数改ページ | `\f` 出力 |
| `0x00`, `0x01`, `0x02` | 半角/上付/下付 | lowをASCIIとして出力 |
| `0xc5` | 制御文字 (0x00-0x1F) | lowをそのまま出力 |
| `0xfe` | ブロック開始（次の0xfeまでスキップ） | skipフラグON |
| `0x21`〜`0x7E` | 全角文字のJIS第1バイト | バイトスワップしてデコード |

---

## 4. 新松フォーマット（BUN2TX.Cより）

```c
fseek(in_fp, 266, 0);   // 本文へジャンプ
fread(mchar, 2, 1, in_fp);  // 2バイト読む
low  = mchar[0];  // JIS第2バイト
high = mchar[1];  // JIS第1バイト
if(high == 0x84) break;  // 文書終了
```

属性バイトはなく、2バイトが文字コードそのもの。Shift-JIS変換は `tosj(low, high)` 関数で行う（標準JIS→SJIS変換アルゴリズム）。

---

## 5. 推奨デコーダ（松86/松5/松6用）

```python
def decode_matsu_v56(filepath):
    with open(filepath, 'rb') as f:
        data = f.read()

    title = data[0x07:0x2F].decode('cp932', errors='replace').rstrip('\x00')

    def is_jis_pair(b0, b1):
        return 0x21 <= b0 <= 0x7E and 0x21 <= b1 <= 0x7E

    offset = 0x019E  # 松86/5/6の本文開始
    result = []

    while offset < len(data) - 1:
        b0, b1 = data[offset], data[offset + 1]

        if b0 == 0x00 and b1 == 0x80:   # 改行
            result.append('\n')
            offset += 2
            continue
        if b0 == 0x00:                   # その他制御コード
            offset += 2
            continue
        if is_jis_pair(b0, b1):          # 全角文字
            try:
                esc = b'\x1b$B' + bytes([b1, b0]) + b'\x1b(B'
                result.append(esc.decode('iso-2022-jp'))
            except:
                result.append('?')
            # 属性バイトをスキップ（次の有効位置までスキャン）
            offset += 2
            while offset < len(data) - 1:
                nb0, nb1 = data[offset], data[offset + 1]
                if nb0 == 0x00 and nb1 == 0x80: break   # 次が改行
                if is_jis_pair(nb0, nb1): break          # 次の文字
                offset += 1
            continue
        offset += 1  # 不明バイト→スキップ

    return title, ''.join(result)
```

---

## 6. 既知の制限

- JIS X 0208 範囲外の文字（JIS X 0212補助漢字、外字）は文字化けする。漢文の希少字で発生。
- 罫線・表組みは無視される（BUN2TX.Cも同様）。
- 脚注・注釈の格納位置は未解明。
- 新松形式には別のデコーダが必要（BUN2TX.C参照）。

---

## 7. 代替ツール

- **BUN2TX**（村井安雄氏）: 新松専用。ソースあり（BUN2TX.C）。コンパイルすれば動く。
- **xdoc2txt**（hishida氏）: 新松/松5/松6に対応。Windows用バイナリ。ソース非公開。
- **リッチテキスト・コンバータ**（アンテナハウス）: 商用。

---

*本文書は管理工学研究所の知的財産に関する公式文書ではなく、バイナリ解析と BUN2TX.C のソースコード照合に基づく技術的記録です。*
